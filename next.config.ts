import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["coin-images.coingecko.com", "images.complex.com", "bitcoinist.com", "grahamcluley.com", "www.newsbtc.com", "www.rawstory.com"],
  },
};

export default nextConfig;
